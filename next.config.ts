/**
 * Next.js configuration for frozensapphire.
 * Extend this file as features (images, rewrites, etc.) are enabled.
 */
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Placeholder for future config (images, headers, rewrites, etc.).
};

export default nextConfig;
